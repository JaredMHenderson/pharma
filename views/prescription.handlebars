<div class="container">
  <div class="row">
    <h1 class="headline">New Prescription</h1>
  </div>
  <div class="row">
    <div class="col-lg-8">
      <form>
        <div class="form-group">
          <label for="rxName" class="subHead">Drug:</label>
          <input type="text" class="form-control" id="rxName" placeholder="Name">
        </div>
        <div class="form-group">
          <label for="rxCatagory" class="subHead">Drug Category:</label>
          <input type="text" class="form-control" id="rxCategory" placeholder="Category">
        </div>
        <div class="form-group">
          <label for="rxQuantity" class="subHead">Drug Quantity:</label>
          <input type="number" class="form-control" id="rxQuantity" placeholder="Quantity">
        </div>
        <div class="form-group">
            <label for="pa   ientComments" class="subHead">Additional Comments:</label>
              <div class="input-group">
                <textarea class="form-control" aria-label="With textarea"></textarea>
              </div>
          </div>
            <a class="btn btn-outline-dark btn-lg" onclick="createPrescription()" href="home" role="button">Submit Prescription</a>
      </form>
    </div>
  <script>

    function createPrescription(){
      var newPrescription = {
        name: $('#rxName').val().trim(),
        category: $('#rxCategory').val().trim(),
        stock: $('#rxQuantity').val().trim(),
      }
      prescriptions.createPrescription(function(result){
        alert(result);
      }, newPrescription);
    }
  </script>